*/
function MYMODULE_views_pre_view(&$view, &$display_id, &$args){
  if ($view->name == 'TestScroll' && $display_id == 'DISPLAYNAME'){
    $header = 'testscroll';
    $options = array(
      'empty' => 1,
      'content' => $header, 
      'label' => NULL,
    );
    $view->add_item('DISPLAYNAME', 'header', 'views','area_text_custom', $options);
  }
}*/